I will remove `liquid-glass-react` from your project and replace it with standard CSS to maintain the glassmorphism effect without the external dependency.

### **Step 1: Replace Usage in Components**
I will update the following files to replace the `<LiquidGlass>` component with a standard HTML `<div>` styled with CSS `backdrop-filter` to replicate the glass effect.

*   **`frontend/src/components/AnnouncementBar.jsx`**
    *   Remove `import LiquidGlass`.
    *   Replace the main container and the close button wrapper with `<div>` elements.
    *   Add inline styles for `background`, `backdropFilter`, `border`, and `boxShadow`.

*   **`frontend/src/components/AnnouncementBanner.jsx`**
    *   Remove `import LiquidGlass`.
    *   Replace the main banner, pagination dots, and close button wrappers with `<div>` elements.
    *   Apply similar glassmorphism styles.

### **Step 2: Remove Dependency**
*   Run `npm uninstall liquid-glass-react` in the `frontend` directory to remove the package and update `package.json`.

### **Verification**
*   I will verify the changes by ensuring the build runs and the components still render with a similar visual style.
